import { PipelineType } from "@huggingface/transformers";
export declare enum UsePipelineStatus {
    PRELOAD = 0,
    LOADING = 1,
    READY = 2
}
declare const usePipeline: <PayloadType = any, ResultType = any>(task: PipelineType, model_id: string, options?: Record<string, any>, worker?: Worker) => {
    pipe: (data: PayloadType, options?: Record<string, any>) => Promise<ResultType>;
    status: UsePipelineStatus;
    progress: number;
};
export default usePipeline;
